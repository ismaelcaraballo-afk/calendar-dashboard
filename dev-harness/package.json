{
  "name": "calendar-dashboard-dev-harness",
  "version": "1.0.0",
  "description": "Standalone dev harness for testing the restv1api/ credentials endpoint without the full Cal.com monorepo.",
  "private": true,
  "scripts": {
    "dev": "tsx --tsconfig tsconfig.json server.ts",
    "dev:watch": "tsx watch --tsconfig tsconfig.json server.ts"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
