{
  "name": "@calendar-dashboard/prisma",
  "version": "1.0.0",
  "description": "Prisma schema and generated client for the Calendar Dashboard — Connected Apps Security Feature",
  "scripts": {
    "generate": "prisma generate",
    "db:migrate": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}
